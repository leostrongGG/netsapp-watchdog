# ===== IDENTIFICAÇÃO DA VPS =====
VPS_NAME="Minha VPS"                                # ← Nome amigável para identificar esta VPS nas notificações
BACKEND_PUBLIC_URL="https://app.exemplo.com.br"     # ← URL pública do sistema (enviada no webhook)

# ===== CONFIGURAÇÕES PRINCIPAIS =====
TICKETZ_DIR="/home/ubuntu/ticketz-docker-acme"      # ← Diretório do Docker Compose do Ticketz
BACKEND_CONTAINER="ticketz-docker-acme-backend-1"   # ← Nome do container do backend
BACKEND_PORT=3000                                   # ← Porta interna do backend (URL é construída automaticamente)
RETRIES=3                                           # Tentativas de health check antes de considerar falha
RETRY_DELAY=10                                      # Segundos entre tentativas

# ===== CONFIGURAÇÕES DE BACKUP DE LOGS =====
SAVE_BACKEND_LOGS=true                              # true = salva logs | false = não salva
BACKUP_TYPE="TAIL"                                  # FULL = log completo | TAIL = últimas N linhas
BACKUP_TAIL_LINES=10000                             # Número de linhas finais a salvar

# ===== CONFIGURAÇÕES DE NOTIFICAÇÃO VIA WEBHOOK =====
WEBHOOK_URL=""                                      # ← URL do webhook (n8n, Make, Zapier, etc)
                                                    # Deixe vazio para desabilitar notificações
WEBHOOK_AUTH_HEADER=""                               # ← Token de autenticação (ex: Bearer seu_token)
                                                    # Deixe vazio ("") se não usar autenticação

# ===== SISTEMA DE LOCK =====
LOCK_FILE="/tmp/netsapp-watchdog.lock"
LOCK_TIMEOUT=600                                    # 10 minutos (tempo máximo que o script pode rodar)

# ===== PROTEÇÃO CONTRA CONFLITOS DE UPDATE =====
UPDATE_DETECTION_WAIT=30                            # Segundos para aguardar e confirmar se é update ou crash

# ===== COOLDOWN: EVITAR AÇÕES REPETIDAS =====
COOLDOWN_FILE="/tmp/netsapp-watchdog-cooldown"
COOLDOWN_SECONDS=900                                # 15 minutos — não tenta recuperar novamente nesse período
